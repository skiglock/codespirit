import id from '../fields/id'
import title from '../fields/title'

const portfolio = {
  name: 'portfolio',
  label: 'Портфолио',
  widget: 'object',
  fields: [id, title]
}

export default portfolio
