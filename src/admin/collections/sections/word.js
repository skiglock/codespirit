import id from '../fields/id'
import description from '../fields/description'

const word = {
  name: 'word',
  label: 'Любой текст',
  widget: 'object',
  fields: [id, description]
}

export default word
