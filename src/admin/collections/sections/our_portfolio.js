import id from '../fields/id'
import title from '../fields/title'

const ourPortfolio = {
  name: 'ourPortfolio',
  label: 'Портфолио',
  widget: 'object',
  fields: [id, title]
}

export default ourPortfolio
